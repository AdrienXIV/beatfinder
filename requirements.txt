# Web stack
fastapi>=0.115.0,<0.130.0
uvicorn[standard]>=0.32.0,<0.40.0
pydantic>=2.9.0,<3.0.0
pydantic-settings>=2.6.0,<3.0.0

# Spotify
spotipy>=2.24.0,<3.0.0

# Audio download (V1)
yt-dlp>=2024.10.0

# Audio analysis
librosa>=0.10.2,<0.12.0
soundfile>=0.12.1,<0.14.0
pyloudnorm>=0.1.1,<0.2.0
numpy>=1.26,<3
scipy>=1.13,<2
mutagen>=1.47,<2

# Multipart form-data (FastAPI UploadFile)
python-multipart>=0.0.9,<1

# Persistence
SQLAlchemy>=2.0.0,<3.0.0

# Config
python-dotenv>=1.0.0,<2.0.0

# macOS — NSApplicationDelegate pour capturer les clics Dock (handler reopen)
# Sans ça, clic Dock active Chrome.app qui ouvre une New Tab vide. Avec, on
# route le reopen vers _focus_existing_window qui raise la fenêtre Chrome --app.
pyobjc-core>=10.0; sys_platform == "darwin"
pyobjc-framework-Cocoa>=10.0; sys_platform == "darwin"

# Dev
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0
ruff>=0.6.0,<1.0.0
